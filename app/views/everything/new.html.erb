<%= form_for @todo, url: {action: "create"} do |f| %>
  <%= f.label :tasks %>:
  <%= f.text_field :tasks %><br />

  <%= f.label :finished? %>:
  <%= f.text_field :finished, placeholder: @placeholder %><br />

  <%= f.submit %>
<% end %>
